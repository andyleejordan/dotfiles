#!/bin/bash
LOCK=~/.duply/verify.lock
export PATH=/usr/local/bin:$PATH
ulimit -n 1024
source ~/.backuppass

if lockfile -600 -r 3 $LOCK; then
    duply slartibartfast verify_purge --force
    duply krikkit verify_purge --force
    rm -f $LOCK
fi
